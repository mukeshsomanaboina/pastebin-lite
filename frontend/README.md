# Pastebin-Lite

A simple Pastebin-like application built with React + Node.js.

## Run locally

### Backend
cd backend
npm install
npm run dev

### Frontend
cd frontend
npm install
npm run dev

## Persistence
Redis (Upstash / local Redis)

## Notes
- Supports TTL and max views
- Deterministic testing via x-test-now-ms
